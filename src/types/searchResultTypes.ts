export type searchResultsType = {
    title: string,
    price: string,
    image: string
}

export type searchResultsCardType = {
    id: string
    title: string,
    category: string,
    price: string,
    image: string
}
