export type searchResultsType = {
    title: string,
    author: string,
    image: string
}

export type searchResultsCardType = {
    title: string,
    category: string,
    price: string,
    image: string
}
