export type searchResultsType = {
    id: string,
    title: string,
    price: string,
    image: string
}

export type searchResultsCardType = {
    id: string
    title: string,
    category: string,
    price: string,
    image: string
}
